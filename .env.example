# Jeton public Mapbox (lecture seule côté navigateur — restreindre par URL dans le compte Mapbox).
# Local : dev + build. Production Workers : même nom dans Variables du worker bouffonsbios.
PUBLIC_MAPBOX_ACCESS_TOKEN=

{
  "name": "bouffonsbio",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types",
    "deploy": "astro build && wrangler deploy",
    "deploy:oauth": "wrangler deploy -c workers/decap-oauth/wrangler.jsonc",
    "import:wp": "node scripts/import-wordpress.mjs"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^13.1.7",
    "astro": "^6.1.3",
    "wrangler": "^4.80.0"
  },
  "overrides": {
    "vite": "^7"
  },
  "devDependencies": {
    "fast-xml-parser": "^5.5.10",
    "turndown": "^7.2.3"
  }
}
